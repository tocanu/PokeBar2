<Window x:Class="Pokebar.DesktopPet.IconOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True">
    <Grid Width="48" Height="48">
        <!-- Simple semi-transparent icon placeholder -->
        <Border x:Name="IconVisual"
                Background="#DD4488FF"
                CornerRadius="4"
                Opacity="0.85"
                Width="40" Height="40"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="4" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>
        </Border>
        <!-- Small icon-name label below (debug/fun) -->
        <TextBlock x:Name="IconLabel"
                   Text=""
                   FontSize="7"
                   Foreground="White"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,-2"
                   Visibility="Collapsed"/>
    </Grid>
</Window>
